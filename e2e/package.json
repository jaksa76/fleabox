{
  "name": "fleabox-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Fleabox",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:auth": "playwright test --config=playwright.auth.config.ts",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}