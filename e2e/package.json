{
  "name": "fleabox-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for Fleabox",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:app": "playwright test --project=app-tests",
    "test:auth": "playwright test --project=auth-tests",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^25.1.0"
  }
}
