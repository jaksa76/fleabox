{
  "name": "Fleabox Rust Dev Container",
  "image": "mcr.microsoft.com/devcontainers/rust:1-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/schlich/devcontainer-features/playwright:0": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "vadimcn.vscode-lldb"
      ]
    }
  },
  "postCreateCommand": "rustc --version && cargo --version && sudo apt-get update && sudo apt-get install -y libpam0g-dev && sudo useradd -m -s /bin/bash alice && echo 'alice:alice123' | sudo chpasswd && sudo useradd -m -s /bin/bash bob && echo 'bob:bob123' | sudo chpasswd",
  "remoteUser": "vscode"
}